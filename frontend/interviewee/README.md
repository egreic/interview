# frontend/interviewee — placeholder (built at J3)

React + Vite, mobile-first, Safari iOS as a design case. Uses the design
system tokens only (`design/rendus/tokens.css`) — the school accent is
injected per tenant, tint and halo are derived from it.
